# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.envrc
.python-version
.claude/
users.db
settings.local.toml
.env
settings.local.yaml
elastic-start-local/
/site
.DS_Store

# Web UI: ui/dist is already covered by the /dist rule above; these are the
# other build/dependency artifacts, plus the staged copy `make ui` puts
# where mcc/routes.py serves it from.
ui/node_modules/
/mcc/static/ui/
# Added by code-review-graph
.code-review-graph/
